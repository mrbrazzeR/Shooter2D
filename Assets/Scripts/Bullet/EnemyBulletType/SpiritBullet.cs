using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpiritBullet : EnemyBullet
{
    // Start is called before the first frame update
    void Start()
    {
        damage = 5;
        speedBullet = 1f;
    }

    // Update is called once per frame
    
}
